# Demo Mode Configuration
# Copy this file to .env.local and set NEXT_PUBLIC_DEMO_MODE=true
# 将此文件复制为 .env.local 并设置 NEXT_PUBLIC_DEMO_MODE=true

# 启用 Demo 模式（使用 mock 数据，无需后端）
NEXT_PUBLIC_DEMO_MODE=true

# API URL（在 Demo 模式下不使用，但保留配置）
NEXT_PUBLIC_API_URL=http://localhost:8000
